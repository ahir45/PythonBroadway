# git config --global user.name ""
# git config --global user.email ""


# git init
# git add .  # git add filename
# git commit -m "<commit message>"
# git remote add origin <giturl>
# git push -u origin <branch_name>     # once used -u, we can use only git push from next time

# -u tracks upstream by syncing local and remote branch


# clear
# git status     #  shows the current status of all git staged and unstaged files

              # to setup a new username/email for particular project
 # git config user.email ""


# SSH key to generate public key for github and private key for local device to communicate between eachother
# ssh-keygen -t rsa -b 4096 -C "your_email@example.com"
# use cmd to generate ssh key using type filename

#### GIT BRANCHING ####

# git <branch_name>          # creates a new branch
# git checkout <branch_name>        # jumps (check outs ) to the next branch
# git branch                         # It shows all the branches and highlights the current branch
# git branch -M main
# git checkout -b <branch_name>          # creates a new branch and jumps to that branch
# main is used instead of master after black lives matter protest


