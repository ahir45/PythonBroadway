a = "Hello World"
print(len(a)) #returns the length of the sequence
print(bool(a)) #here its non empty string which is truthy, so it returns true
print(a[slice(1,5)]) #slice function can be used in the string slicing.
# slice(start, end, step) step can also be provided as the third paramter
print(type(a)) #this returns type of the object inside the function

